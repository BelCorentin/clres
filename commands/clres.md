---
description: List recent Claude Code conversations (emoji + title + project + age)
allowed-tools: Bash(python3 *)
---

Run this command and show the user its raw output as-is (it is already formatted):

!`python3 ${CLAUDE_PLUGIN_ROOT}/clres.py --list`

Then remind the user: for the interactive picker (arrow keys, filter, resume), run `clres` in a regular shell.
